{
  "name": "oltflow",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build -w packages/core && npm run build -w packages/adapters && npm run build -w packages/db",
    "dev": "npm run build:packages && npm run dev -w apps/web",
    "build": "npm run build:packages && npm run build -w apps/web",
    "worker": "npm run build:packages && npm run dev -w apps/worker",
    "db:generate": "npm run generate -w packages/db",
    "db:migrate": "npm run migrate -w packages/db",
    "db:seed": "npm run seed -w packages/db"
  }
}
